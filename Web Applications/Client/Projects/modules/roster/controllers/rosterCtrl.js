var app = angular.module('app.authentication.roster.controller',[
	'app.authentication.roster.calendar.controller',
	'app.authentication.roster.home.controller',
]);

app.controller('rosterCtrl', function($scope, $state, $cookies){
    
});